package com.premiere.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds & Surfaces
val BackgroundDark = Color(17, 17, 17)
val NavBackground = Color(27, 26, 51)
val SurfaceDeep = Color(30, 30, 46)
val SurfaceHeader = Color(26, 26, 46)
val SurfaceCard = Color(32, 32, 56)
val SurfaceElevated = Color(43, 43, 72)
val SurfaceChip = Color(45, 45, 74)
val SurfaceAvatar = Color(42, 42, 62)

// Brand
val PremiereRed = Color(229, 9, 20)
val PremiereRedDark = Color(185, 28, 37)
val PremiereGold = Color(245, 197, 24)
val PremiereBlue = Color(77, 171, 247)

// Elements
val ChipGray = Color(59, 59, 59)
val BorderDark = Color(34, 34, 34)
val SliderInactive = Color(107, 107, 107)

// Text (light → dark)
val TextBright = Color(225, 225, 225)     // Section titles, chip labels
val TextPrimary = Color(221, 221, 221)    // Primary body, overview headings
val TextSecondary = Color(214, 214, 214)  // Secondary labels, genre chips
val TextLabel = Color(209, 209, 209)      // Dropdown/sort labels
val TextBody = Color(187, 187, 187)       // Body copy
val TextCaption = Color(182, 182, 182)    // Captions, arrows
val TextMeta = Color(176, 176, 176)       // Metadata (year, etc.)
val TextMuted = Color(160, 160, 160)      // Muted hints / placeholders
val TextDim = Color(153, 153, 153)        // Dim text
val TextFaded = Color(138, 138, 138)      // Faded text
val TextSubdued = Color(119, 119, 119)    // Subdued (/10, etc.)
